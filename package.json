{
  "name": "bun-raylib",
  "type": "module",
  "scripts": {
    "setup": "bun run scripts/prepare.ts && bun run scripts/gen.ts && bun run scripts/build-bridge.ts",
    "gen": "bun run scripts/gen.ts && bun run scripts/build-bridge.ts",
    "smoke": "bun run test/smoke.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
